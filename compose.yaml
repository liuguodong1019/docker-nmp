services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
      target: php8.2
    volumes:
        - /dockerConfig/php:/usr/local/etc
        - /var/www/html:/var/www/html
    container_name: php
    expose:
      - "9000"
  nginx:
    build:
      context: .
      dockerfile: Dockerfile
      target: nginx
    container_name: nginx
    depends_on:
      - php
    ports:
      - "80:80"
    volumes:
      - /etc/hosts:/etc/hosts
      - /dockerConfig/nginx:/etc/nginx
      - /var/www/html:/var/www/html
  db:
    build:
      context: .
      dockerfile: Dockerfile
      target: mysql
    ports:
      - "3306:3306"
    container_name: mysql
    environment:
      MYSQL_ROOT_PASSWORD: 123456
    volumes:
      - /dockerConfig/mysql/my.cnf:/etc/my.cnf
      #- /dockerConfig/mysql/my.cnf.d:/etc/my.cnf.d
      #- /dockerConfig/mysql/conf.d:/etc/mysql/conf.d
      #数据存储位置
      - /dockerConfig/mysql/data:/var/lib/mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
